import { View, Text } from "react-native";

export default function Map() {
  return (
    <View style={{
      flex: 1,
      justifyContent: "center",
      alignItems: "center"
    }}>
      <Text>Aqui será exibido o mapa.</Text>
    </View>
  );
}